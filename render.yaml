services:
  - type: web
    name: ragx-backend
    env: python
    buildCommand: "pip install --no-cache-dir -r ragx/backend/requirements.txt"
    startCommand: "cd ragx/backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
